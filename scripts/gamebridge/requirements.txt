# GameBridge Python client — pip dependencies
#
# Install: pip install -r scripts/gamebridge/requirements.txt
# Or via mise: mise run gamebridge-setup

# Qt dashboard
PyQt6>=6.6.0

# Testing
pytest>=7.0

# Optional — hull polygon hit-testing (GAMEBRIDGE.md "Canvas coordinates" section)
# shapely>=2.0
